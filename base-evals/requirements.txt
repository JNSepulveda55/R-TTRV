# The cluster RTX PRO 6000 Blackwell GPUs are sm_120. Older Torch/vLLM
# stacks such as vllm==0.8.3 + torch==2.6.0 only ship kernels through sm_90.
vllm==0.23.0
transformers==4.57.6
datasets
matplotlib
pillow
tqdm
